# one time for each repository
git init

# every time you commit or change anything in a file

git add .
git commit -m "first commit"

git branch -M main
git remote add origin https://github.com/RatulDeb/GitHub-Repository-1.git

git push -u origin main


# After creating a repository and after all the steps you have to just follow three 
 commands 

1. git add .
2. git commit -m "Here you will write what you have changed"
3. git push -u origin main